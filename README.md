# jnn_mobile

> **Agent Instructions:** Use Lucide icons (https://lucide.dev/icons) for all icon needs. Do NOT use Flutter's built-in Icons.* or Material icons — always use the `LucideIcons.*` package instead.
